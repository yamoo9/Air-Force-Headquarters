module.exports = {
  // 줄 바꿈 길이 설정
  printWidth: 80,
  // 탭(true), 스페이스(false) 사용 설정
  useTabs: false,
  // 탭(tab) 공백 수 설정
  tabWidth: 2,
  // 작은 따옴표(') 사용 설정
  singleQuote: true,
  // 명령문 끝에 세미콜론 추가 설정
  semi: !true,
  // 화살표 함수 => 단일 매개변수 괄호 () 설정
  arrowParens: 'always',
  // 객체 또는 배열의 여러 줄 입력 시, 후행 쉼표 추가 설정
  trailingComma: 'es5',
  // HTML 공백 감도 설정
  htmlWhitespaceSensitivity: 'ignore',
  // 객체 리터럴 괄호({ }) 사이 공백 출력
  bracketSpacing: true,
}
